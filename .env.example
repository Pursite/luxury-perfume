# Choose the template that matches the selected settings module:
#
#   cp .env.development.example .env  # config.settings.development
#   cp .env.production.example .env   # config.settings.production
#
# This selector intentionally has no variable assignments, avoiding a third,
# duplicate variable inventory. The tracked environment-specific templates
# contain the complete variables for their respective environments.
