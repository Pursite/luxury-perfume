services:

  web:
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ..:/app
    ports:
      - "127.0.0.1:${DJANGO_HOST_PORT:-8000}:8000"

  celery:
    volumes:
      - ..:/app
